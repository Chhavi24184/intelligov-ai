# Data Flow

The full step-by-step data flow narrative and sequence diagram — from citizen query, through input processing, the orchestrator, all agents, the RAG pipeline, the Granite mock layer, the Decision Engine, and back to the UI — lives in [`docs/data-flow.md`](../docs/data-flow.md).
