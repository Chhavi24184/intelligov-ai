# API Flow

All REST endpoints, request/response examples, authentication behavior, and error-handling conventions are documented in [`docs/api-flow.md`](../docs/api-flow.md).

Interactive, always-current documentation is also auto-generated by FastAPI at `http://localhost:8000/docs` when running the backend locally.
